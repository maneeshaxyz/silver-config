clamav {
  type = "clamav";
  servers = "clamav-server:3310";  # Changed from Unix socket to TCP
  action = "add_header";  # or reject / add_header / discard
  symbol = "CLAM_VIRUS";
  log_clean = true;
}
