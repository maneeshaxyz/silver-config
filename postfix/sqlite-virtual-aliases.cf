# Postfix SQLite configuration for virtual aliases
# Query the aliases table to resolve alias to destination user
# Returns destination email address
# NOTE: Using shared.db which contains users, domains, and aliases

dbpath = /app/data/databases/shared.db
query = SELECT REPLACE('%s', 'admin@', 'postmaster@') WHERE instr('%s', 'admin@') = 1
